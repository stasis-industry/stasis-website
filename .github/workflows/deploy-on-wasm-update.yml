name: Deploy on WASM Update

on:
  repository_dispatch:
    types: [wasm-updated]

jobs:
  trigger-vercel:
    name: Trigger Vercel Redeploy
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Vercel deploy hook
        run: |
          curl -X POST "${{ secrets.VERCEL_DEPLOY_HOOK }}"
